function solve(param) {

    if(param == null){
        console.log(typeof param);
        console.log("Parameter is not suitable for printing");
    }else{
        console.log(typeof param);
        console.log(param);
    }
}

solve(null);